{
  "name": "copilot-harness-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Private, loopback-only Windows supervisor for a pinned GitHub Copilot compatibility gateway.",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/node/*.test.mjs",
    "test:runtime": "node --test --test-concurrency=1 tests/node/supervisor.test.mjs"
  },
  "dependencies": {
    "@jeffreycao/copilot-api": "2.0.1",
    "jsonc-parser": "3.3.1",
    "smol-toml": "1.7.1"
  }
}
